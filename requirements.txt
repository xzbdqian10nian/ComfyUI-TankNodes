# The server image already provides these dependencies.  Keep this file
# conservative: ComfyUI Manager must not replace the CUDA llama.cpp wheel
# with the stale CPU wheel from PyPI.
diskcache>=5.6.2,<6
# OpenAI-compatible API support uses the existing OpenAI SDK when available.
# It is intentionally not pinned here to avoid changing the shared image.
# Without the SDK, API requests use aiohttp, which ComfyUI already requires.
